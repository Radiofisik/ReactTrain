import logo from "../../logo.svg";
import React from "react";
import classes from  "./Header.module.css"

const Header = () => {
    return <header className={classes.header}><img src={logo}/> Header </header>
};

export default Header;
